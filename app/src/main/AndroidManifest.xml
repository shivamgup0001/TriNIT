<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trinit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TriNIT">
        <activity android:name=".LeadReportedBugs"></activity>
        <activity android:name=".LeadRequests" />
        <activity android:name=".LeadAddPeople" />
        <activity android:name=".EmployeeUI" />
        <activity android:name=".UserUI" />
        <activity android:name=".OrganisationLead">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>